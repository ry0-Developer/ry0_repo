name: App Builder Bot

on:
  workflow_dispatch:
    inputs:
      app_name:
        description: 'Test App Name'
        required: true
        default: 'TestApp'

jobs:
  build-app:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Test Run
        run: |
          echo "Hello Boss! The bot is working perfectly."
